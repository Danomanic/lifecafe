/**
 * Shared constants used across the application
 */

// Field names that should be skipped when recursively searching menu data
export const MENU_SKIP_FIELDS = ['title', 'description', 'name', 'price'];

// Default button color for menu items
export const DEFAULT_ITEM_COLOR = 'slate';

// Number of tables available
export const TOTAL_TABLES = 12;

// Default quantity for items
export const DEFAULT_ITEM_QUANTITY = 1;

// Minimum quantity for items
export const MIN_ITEM_QUANTITY = 1;
